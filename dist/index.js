import express, {} from "express";
const app = express();
const PORT = 4000;
app.get("/", (req, res) => { });
app.listen(PORT, () => {
    console.log("Server is running on port 3000");
});
//# sourceMappingURL=index.js.map